<h1>Listing ads</h1>

<table>
  <thead>
    <tr>
      <th>Company name</th>
      <th>Duration</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @ads.each do |ad| %>
      <tr>
        <td><%= ad.company_name %></td>
        <td><%= ad.duration %></td>
        <td><%= link_to "Show", show_ad_path(ad) %></td>
        <td><%= link_to "Edit", edit_ad_path(ad) %></td>
        <td><%= link_to "Destroy", destroy_ad_path(ad),
          "data-confirm" => "Are you sure?",
          "data-method" => "delete" %></a></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to "New Ad", new_ad_path %>
